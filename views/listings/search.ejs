<% layout("/layouts/boilerplate") %>

<h1>Search Results</h1>
<% if (listings.length > 0) { %>
  <ul>
    <% listings.forEach(listing => { %>
      <li><%= listing.title %> - <%= listing.location %></li>
    <% }) %>
  </ul>
<% } else { %>
  <p>No listings found.</p>
<% } %>